{
  "name": "expense_api",
  "version": "1.0.0",
  "description": "Proyecto de gastos personales - api",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "docker-compose up -d && node --env-file .env --watch .",
    "start": "docker-compose up -d && node --env-file .env index.js node .s",
    "stop": "docker-compose down"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.9.4",
    "process": "^0.11.10",
    "sequelize": "^6.37.1"
  }
}
